# Generated gRPC client SDKs live under this package (one sub-package per target
# service, e.g. clients/trust). Each sub-package is produced by
# `xime grpc client` and must not be edited by hand.
# Thư mục chứa các SDK client sinh tự động (mỗi service đích một sub-package, ví
# dụ clients/trust). Mỗi sub-package do `xime grpc client` sinh ra, không sửa tay.
